[project]
name = "fules-online"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "feedparser",
    "beautifulsoup4",
    "requests",
    "anthropic",
    "pillow",
]

[tool.vercel]
entrypoint = "api.index:app"
